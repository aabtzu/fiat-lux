services:
  - type: web
    name: fiat-lux
    runtime: node
    region: oregon
    rootDir: visualizer
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
